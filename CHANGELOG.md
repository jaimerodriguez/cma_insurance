# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added
- 2026-07-25: **`tests/test_triage.py` — 47 tests for the triage path**, the first coverage of the domain logic (`tests/` previously held only `agent_obs` tests). Covers auto-approve ceiling selection (baseline / VIP / home / upset / agent override, and the boundary being exclusive), routing to the lowest qualifying adjuster across all bands, the `can_assign` gate, escalation to a claim's own adjuster, the whole management path (cost-driven and adjuster-driven, `can_assign` independence, idempotent re-triage, override precedence, queue withdrawal), terminal-resolution auto-close, and the `AuthorizationLevel` band mapping.
  - **`tests/conftest.py`** adds a `world` fixture that redirects the storage layer at a scratch `data/` seeded with one adjuster per authorization band. It patches every `storage` path constant, not just `DATA_DIR` — the file globals are bound at import, so patching the directory alone leaves reads and writes pointed at the repo's real `data/`. It also puts the project root on `sys.path`, so a bare `pytest tests/` works and not only `python -m pytest`.
  - **Verified by mutation rather than by passing.** Eight regressions were reintroduced one at a time against a scratch copy and the suite had to catch each: `required_authorization` returning `None` again (8 failures), `is_home` narrowed back to the policy type (5), the idempotency guard removed (2), `MANAGEMENT_OVERRIDE` dropped from `is_terminal` (2), the management check moved behind the `can_assign` gate (1), `Adjuster` no longer rejecting `OVERRIDE_NEEDED` (1), and the auto-approve comparison made inclusive (1).
  - The eighth mutation — letting routing select the `unassigned` placeholder — initially **passed**, exposing a vacuous test: with a real LOW adjuster in the roster, the `user_id` tie-break (`"leo" < "unassigned"`) picks the right adjuster whether or not the guard exists. The test now leaves the placeholder as the only LOW candidate, so it fails when the guard is removed.
- 2026-07-25: **`ReportStatus.ESCALATED_TO_MANAGEMENT` and `AuthorizationLevel.OVERRIDE_NEEDED`**, plus the routing that connects them to last change's `Resolution.MANAGEMENT_OVERRIDE`. The full path is now: cost above every band → `OVERRIDE_NEEDED` → status `ESCALATED_TO_MANAGEMENT` → resolution `MANAGEMENT_OVERRIDE` (terminal) → auto-close.
  - **`required_authorization` no longer returns `None`.** Above `authorization_high` it returns `OVERRIDE_NEEDED`; the return type is now plain `AuthorizationLevel`. "Beyond every band" is a real answer the caller must route on, and as `None` it was indistinguishable from "no adjuster is available" — both fell into the same `return incident` and the claim was silently stranded with nobody able to decide it.
  - **`process_incident` checks for `OVERRIDE_NEEDED` before any routing**, ahead of the `can_assign` gate on purpose: `can_assign=False` means "don't pick an adjuster", not "strand a claim nobody may decide". This applies whether or not the incident is already assigned.
  - **New tool `escalate_to_management(incident_id, reason)`** (31 tools now), granted to ADJUSTER and AGENT. Sets the status, stores the reason, logs history, and withdraws any pending adjuster escalation since the decision is no longer theirs. Deliberately *not* a resolution of its own — an escalation is a routing step, and making it an outcome would stamp `resolved_date` and let the auto-close pass close a claim nobody has decided.
  - **`AuthorizationLevel` gained `.rank` and `.is_assignable`,** replacing the `_AUTH_RANK` lookup table in `tools.py` — which would have raised `KeyError` on the new member. `Adjuster.__post_init__` now rejects `OVERRIDE_NEEDED`, so an adjuster ranked above HIGH can never become the routing candidate for claims no one is allowed to decide.
  - **`process_incident` is idempotent for claims already with management** — it returns them untouched. Without this the churn was real and I verified it: these claims stay `INPROGRESS`, so they keep matching the "open incidents" filter, and three triage passes wrote three identical history lines. The guard also outranks `agent_override_autoapprove`, since an escalation to management is precisely the case where the agent stops deciding.
  - `repl.py` distinguishes a fresh escalation from one still pending ("still with management") so a repeated pass does not claim to have escalated the same claim again.
  - No migration: `data/` holds only statuses new/open/closed and adjuster levels low/medium/high.
- 2026-07-25: **Three home-loss `ReportType` flags** — `HOME_THEFT` (8), `HOME_ACCIDENT` (16), `HOME_NATURAL_DISASTER` (32), joining `STOLEN_CAR`/`STOLEN_OTHER`/`CAR_ACCIDENT`. Plus a `HOME` composite alias (`HOME_THEFT | HOME_ACCIDENT | HOME_NATURAL_DISASTER` = 56) so callers can ask "is this a dwelling loss at all?" without naming each member. The alias is deliberately not a canonical member: `Flag` iteration skips aliases, so `agent_schemas` still advertises exactly the six combinable flags and the generated tool description is unchanged in shape.
  - **`is_home` is now broader than the policy type.** `process_incident` computed it as `policy.policy_type == PolicyType.HOME`; it is now that **or** any `ReportType.HOME` flag on the incident. A dwelling loss filed against a non-HOME policy therefore reaches `vip_auto_approve_home` / `upset_auto_approve_home` instead of being treated as a car claim. This raises the auto-approve ceiling for claims that did not previously qualify — verified: a VIP's 9000 claim on an AUTO policy escalates as `CAR_ACCIDENT` (ceiling 2000) and auto-approves as `HOME_THEFT` (ceiling 25000).
  - Existing bitmasks are untouched: `data/` holds only 1, 2, 4 and 5, all still valid, so no migration is needed.
- 2026-07-25: **`Resolution.MANAGEMENT_OVERRIDE`** — management settled a claim outside the normal authorization bands. Serialized as `"management_override"`. It is a **terminal** outcome: it stamps `resolved_date`, clears any pending `Escalation`, and is eligible for the `close_stale_resolved` auto-close pass, exactly like APPROVED and DENIED.
  - **Added `Resolution.is_terminal`** rather than extending three tuples. `tools.py` spelled the rule as `resolution in (Resolution.APPROVED, Resolution.DENIED)` in three places — `update_resolution` twice (stamping `resolved_date`, popping the escalation) and `close_stale_resolved` once — so adding a member meant remembering all three or getting a resolution that silently never closes. The rule now lives on the enum, next to the members it classifies, and the call sites read `resolution.is_terminal`. `INPROGRESS` and `DISPUTED` remain non-terminal, unchanged.
  - Verified end-to-end on a copy of `data/`: an escalated claim set to `management_override` gets `resolved_date` set, leaves the escalation queue, and is picked up by `close_stale_resolved`; a `disputed` claim still gets neither.
  - `repl.py` needed no change — `process_incident` only ever sets `APPROVED`, and its `INPROGRESS` filter correctly excludes a terminal override.

### Fixed
- 2026-07-25: **`gen_cma_yaml.py` crashed with `AttributeError: module 'cma' has no attribute 'MODEL'`.** `cma.py` now selects the model per role (`MODEL_BY_ROLE`), but the generator still read a single `cma.MODEL`, so the checked-in control plane could not be regenerated at all. It now reads `cma.MODEL_BY_ROLE[role]`, matching what `cma.setup()` sends at agent-create time. Regenerating also flushed drift that had accumulated while the generator was broken: `cma/agent.agent.yaml` picked up six tools the AGENT role had gained (`assign_incident`, `find_adjuster`, `get_adjuster_details`, `list_adjusters`, `process_incident`, `retrieve_policies`) and each agent now carries its intended model (`agent` opus-4-8, `adjuster` sonnet-5, `insurer` haiku-4-5).

### Added
- 2026-07-24: **`agent_obs/shape.py` — tool-definition shaping on the wire path** (`--obs-wire-tools {full,skeleton,names}`, `OBS_WIRE_TOOLS`; `--obs-wire-tools-keep`, `OBS_WIRE_TOOLS_KEEP`). Opt-in, `full` by default. `skeleton` reduces each tool definition to `{name, params, required, desc_chars, sha}` (~55 bytes); `names` to `{name, sha}`; `keep` holds `fnmatch` patterns exempted from both.
  - **Why.** On the Agent SDK path the `claude` CLI assembles the tool list and ships every native tool plus every MCP server configured on the machine — not only the ones this app registers. Measured on `repl-20260724-161108-7658e9.wire.jsonl`: 118 definitions, 268 KB per request, **95% of the body**, of which 8 tools (4.4 KB) were `mcp__insurance__*`. Worse than the bytes, third-party servers interpolate account state into their descriptions: that capture contained a real Slack user id (15 occurrences) and workspace archive URLs, written to disk under `flow`, `dev`, and `none` alike.
  - **A separate layer from redaction, on purpose.** A `Redactor` decides *how much of a value* to keep and knows nothing about the request schema, which is what lets one mode serve the event log and the wire log. A `Shaper` knows exactly what a `/v1/messages` body is and decides *which parts are worth keeping at all*. Folding tool-name matching into the generic walker would have given `StrictRedactor` a special case it has no use for.
  - **Shapers run before the redactor** (`CaptureProxy._record`), for three reasons: a skeleton's `sha` must cover the original tool, and by the time `flow` is done the description is already a `seen` marker; `strict` summarises `body` wholesale so it must be handed a real structure; and the redactor staying last keeps credential stripping unconditional — a shaper is never a second path to disk. Shaping applies to a parsed copy, while the bytes forwarded upstream are the originals read before any of it runs, so it cannot alter the request the model receives. A shaper that raises degrades to a verbatim capture with `shape_error` set, rather than losing the row.
  - **`keep` takes patterns, not names.** A hand-maintained name list silently drops the schema of any tool added later — exactly the schema you would want. One pattern for the server this app registers (`mcp__insurance__*`) covers tools added next month automatically.
  - **Elision stays auditable.** The per-tool `sha` is over the original canonical JSON, so "did the tool list change between turns?" is still answerable from a shaped trace, and rows are stamped `"_shaped": ["tools:skeleton"]` so a filtered capture can never be mistaken for a faithful one.

### Fixed
- 2026-07-24: **`flow` redaction rewrote resent tool schemas in full on every request**, despite the mode's documented claim that "resent tool schemas … collapse for exactly the same reason the message history does." Only the *descriptions* collapsed. Dedup was per-string, and every string inside a JSON Schema is a field name far below `min_collapse_chars` — so `input_schema` (157 KB of the 268 KB tool payload on the measured run) was written verbatim every turn and no per-string floor could ever catch it. `FlowRedactor` now dedups **subtrees** as well: a dict or list whose canonical JSON exceeds `min_node_chars` and has been written before collapses to `{"_t": "seen_node", "kind", "chars", "sha"}`. Hashing is order-independent (`sort_keys`), registration happens on first sight before children are walked so nested repeats still collapse, and depth 0 is exempt so a row never loses its envelope.
  - `min_node_chars` defaults to 500, above the 200-char string floor: collapsing a string costs its text, collapsing a container costs its *shape* too, and shape is most of what a wire trace is read for.
  - On by default — it is lossless and the `sha` is greppable back to the full copy — with `--no-collapse-structures` / `OBS_COLLAPSE_STRUCTURES=0` as the escape hatch.
  - Replaying the 6-request capture through the real pipeline: **1,059 KB verbatim → 570 KB with `flow` as it was → 263 KB with subtree dedup → 59 KB adding `skeleton` + `keep`.** Per-turn marginal cost of the tool list drops from ~123 KB to ~75 bytes; the shaper's remaining win is on the one-time first sighting, which is why the two are separate switches.
  - Noted while measuring, not fixed here: `strict` now costs about the same as `flow` (263 KB) while showing far less, because `StrictRedactor.wire` summarises wholesale and does not dedup at all.
- 2026-07-24: **`agent_obs` event-field name collisions.** `EventLog.debug/info/warn/error/event` raised `TypeError: got multiple values for argument 'name'` whenever a caller logged a field called `name` — which `trace_callable` did on every deterministic-path call (`/agent-run`, `/agent`, `cma.run_agent_maintenance`), crashing them. Fixed structurally rather than per call site: the event-name parameter on all five `EventLog` methods is now **positional-only** (`name: str, /`), as are `Observability.turn(kind, /)` and `SessionTracer.session/turn/tool`, so an arbitrary field can never shadow a parameter. `Observability.turn` also merges its fields into a dict instead of `kind=kind, **attributes`, so an attribute named `kind` overrides rather than raising. `trace_callable` now logs `func=` instead of `name=`.
  - Found a second bug of the same family while testing: a field named `event`, `ts`, `seq`, `level`, `run_id`, or `session_id` silently **overwrote the row's own envelope** — losing the event name made rows unfindable. The envelope now always wins and a colliding field is preserved under a `field.<key>` prefix, so nothing is lost either way.
- 2026-07-24: **`flow` redaction collapsed short labels.** A 22-char function name was being replaced by a `{"_t": "seen", …}` object roughly three times its length. Added `min_collapse_chars` (default 200, `OBS_MIN_COLLAPSE_CHARS`): strings below the floor are always written verbatim however often they repeat, since a collapsed entry serialises to ~70 characters and short repeated strings are labels (tool/function/model/session ids), not content. Content worth deduplicating is far above the floor.

### Changed
- 2026-07-24: **`agent_obs` redaction: new `flow` mode, now the default** (was `strict`). `FlowRedactor` writes each string **in full the first time it appears** (capped at `max_field_chars`, raised 2000 → 4000 so a typical tool description or system prompt fits whole) and collapses every later appearance of the same text to `{"_t": "seen", "preview": <first 20 chars>, "chars": N, "sha": …}`, with `truncated` for over-long first sights. Deduplication is by content hash across the whole run rather than by position, because the same text moves index between requests and because resent tool schemas collapse for the same reason the message history does. One redactor per run, so the event and wire layers share the dedup set; the `sha` on a collapsed entry matches the `sha` the full text was written under, making the first occurrence greppable in the same file.
  - Rationale: `strict` was the wrong default in practice. It hashed our *own* tool schemas and system prompts — static app text, never policyholder data — which made request flows unreadable and defeated the token-cost analysis the wire layer exists for. `flow` keeps every message in the array visible, shows what is new in each request, and collapses the resent history to a followable skeleton; it also bounds the quadratic file growth. `strict` (no content at all) remains available for traces that might leave the machine.
  - `REDACT_MODES` is now `("flow", "strict", "dev", "none")`; added `ObsConfig.preview_chars` (default 20) and `OBS_PREVIEW_CHARS`. `--obs-redact` on both front-ends accepts the new mode. Verified on a live 4-request session: request 1 full, requests 3–4 showing all 4–6 messages with priors previewed and only new content in full.

### Added
- 2026-07-24: **`agent_obs/` — observability package**, wired into both front-ends. Four independently switchable append-only-JSONL layers: `events` (semantic event log), `spans` (OpenTelemetry `session → turn → tool`), `wire` (local capture proxy recording the real HTTP payloads), and `usage` (per-turn token/cost ledger at `var/ledger.jsonl`). One facade — `agent_obs.Observability.start(cfg, front_end=...)` as a context manager — exposing `.events`, `.spans`, `.wire`, `.usage`, plus `obs.turn(kind, **ctx)` and `obs.record_turn(span, record)`. Modules: `facade.py`, `config.py` (`ObsConfig` + `OBS_*` env vars), `events.py`, `spans.py`, `wire.py`, `usage.py`, `redact.py`, `sinks.py`, `tooltrace.py`, `sdk.py`, `logbridge.py`.
  - **All three LLM paths instrumented.** Agent SDK backend: all 10 hooks (`agent_obs.build_hooks`, tracing-only — every callback returns `{}`), message-stream observation (`observe_sdk_message` — `tool_use_id`, thinking sizes, `init` payload), `stderr` into the event log, and one ledger row per `ResultMessage`. `--use-key` backend: one turn span over the whole tool loop, one ledger row per `messages.create` with `extra.loop_turn`. Managed Agents (`cma.py`): per-session-event tracing, usage read defensively off `session` events, `extra.stream_legs`.
  - **Tool tracing is backend-independent.** `repl._dispatch` carries `@agent_obs.traced_dispatch`, which resolves the active run via `agent_obs.current()` at call time — so the Agent SDK path (in-process MCP handlers), the `--use-key` local loop, and `cma.py` (which imports `_dispatch`) are all traced with no changes at the call sites, and the decorator is a no-op when no run is active. Deterministic paths (`run_agent_maintenance`, `run_agent_route_and_triage`, `cma.run_agent_maintenance`) use `@agent_obs.trace_callable`.
  - **Wire capture covers Managed Agents too.** Path matching is configurable (`/v1/messages`, `/v1/beta` by default), so pointing the `anthropic` client at `obs.base_url` records session traffic as well; the Agent SDK path is routed via `ANTHROPIC_BASE_URL` in `ClaudeAgentOptions.env`. Responses are captured at `none`/`summary`/`full` (SSE summarised by event-type counts unless `full`). Auth headers are forwarded upstream but never written.
  - **Redaction at the sink boundary** so a call site can never forget to redact. Credentials are dropped in every mode including `none`; diagnostic strings (`error`, `proxy_error`, `line`) are truncated to 300 chars rather than hashed so traces stay debuggable. Modes: `flow` (default — see Changed above), `strict` (sizes and hashes only), `dev` (truncated content), `none` (passthrough).
  - **CLI/REPL surface.** `repl.py` gains `--wire`, `--obs-redact {flow,strict,dev,none}`, `--no-obs`, `--obs-log-bridge`; `cma.py` gains `--wire`, `--obs-redact`, `--no-obs`. Both gain `/obs`, `/obs tail [n]`, `/obs stats [backend|role|model|kind]`.
  - Verified live end-to-end: an SDK-backend turn with `--wire` produced correlated `sdk.tool_use` → `hook.tool_pre` → `tool.start`/`tool.end` → `hook.tool_post` events, a `session → turn.chat → tool.list_incidents` span tree, a ledger row, and 3 captured requests with zero claim text on disk. The `--use-key` and Managed Agents paths were verified against fake upstreams/clients.
  - Added `opentelemetry-sdk>=1.44.0` to `requirements.txt` (optional — without it the span layer degrades to a no-op tracer and the other three layers still work) and `pytest>=8.0` to `requirements-dev.txt`. Added `var/` to `.gitignore`.
- 2026-07-24: `tests/test_agent_obs.py` — 38 tests for `agent_obs`, weighted toward what is dangerous when wrong: capture-proxy credential non-leakage (parametrized over all four redaction modes, so the most permissive one cannot hide a regression), SSE relay, path filtering, Managed Agents path coverage, self-target rejection, wire-failure containment, all four redaction modes incl. flow-mode dedup/preview/hash-join behaviour, sink rotation and torn-line tolerance, the three ledger adapters, `schema_version` forward-compatibility, span hierarchy, and event ordering. One caught a real defect before it shipped: strict mode was hashing `error` fields, which would have made every trace useless for debugging.
- 2026-07-24: `docs/observability-design.md` — working design document for bringing tracing/logging/usage accounting into this project. Records a review of the four observability layers in `../agent_transcriber2` (event log, OTel spans, wire-capture proxy, usage ledger) with implementation details, what to keep, and the defects to fix before replicating (duplicated pending-rename logic, no redaction, unbounded `requests.jsonl` growth, flat span hierarchy, no tests). Proposes a reusable `agent_obs` package. Section 5 records two findings verified against the installed `claude-agent-sdk 0.2.126`: hooks work on the one-shot `query()` path (it runs streaming internally; only `can_use_tool` requires an `AsyncIterable` prompt), and every tool call in all three backends funnels through `repl._dispatch`, making the tool-function boundary a backend-independent tracing seam. Section 7 now carries the decision log for the implementation that followed (14 decisions, incl. run-id file keying, wire coverage for all three backends, and `flow` redaction), and Section 8 records the first finding the wire layer produced: 130 tool schemas shipped on a single turn against the ~10 the role allows, because `allowed_tools` gates calling rather than shipping.
- 2026-07-24: `retrieve_policies(insurer_id)` in `tools.py` — lists every `Policy` held by an insurer (empty list if none). Registered in `AGENT_TOOLS` and granted to the `INSURER` and `AGENT` roles (`roles.py`). Regenerated `agent_tools_schema.json` (30 tools).
- 2026-07-24: Second LLM backend for `repl.py` selected by `--use-key` (argparse in `main`, default `False`). Off (default) drives the LLM through the **Claude Agent SDK** (`claude-agent-sdk`), which runs the `claude` CLI and authenticates via its subscription — `.env` is not loaded and `ANTHROPIC_API_KEY` is popped from the environment so no API key is used. On (`--use-key`) keeps the existing Anthropic-API path (loads `.env`). `Session` now dispatches `chat()` to `_chat_api` or the async `_chat_sdk` (run via `asyncio.run` per turn); `_build_sdk_server` wraps each role's allowed tools (from `roles.py`) as an in-process MCP server (`mcp__insurance__<fn>`), gated with `permission_mode="dontAsk"` + `allowed_tools` so only those tools are callable. Multi-turn continuity via `resume=<session_id>`; `assume`/`reset` clear it. Startup prints the active backend. Verified live end-to-end (subscription auth, tool calls, multi-turn resume). Updated the module docstring and README.
  - Added `claude-agent-sdk` to `requirements.txt` (needed by the default backend; requires the `claude` CLI installed and logged in).
- 2026-07-24: `requirements.txt` (runtime: `anthropic`, `python-dotenv`) and `requirements-dev.txt` (includes runtime via `-r`, plus `PyYAML` for the `gen_cma_yaml.py` generator). Updated the README setup steps to install via these files.
- 2026-07-24: `README.md` — project overview, architecture, domain model, roles/RBAC, the auto-approval + authorization-routing logic, setup, and usage for both front-ends (`repl.py` test bed incl. dry runs, and `cma.py` Managed Agents), with a file/data map. Distilled from the codebase and `CHANGELOG.md`.
- 2026-07-24: `/agent-run --dry-run` (also `-n`) in `repl.py` — previews route + triage + close by executing the real code path inside a `data/` snapshot/rollback (`_rolled_back_data` context manager), so nothing is persisted. The run is re-runnable and safe to compare across policies; report wording switches to conditional ("would route & escalate …") and is tagged `[DRY RUN — no changes written]`.
- 2026-07-24: Made `repl.py` a local test bed for the AGENT persona. New `/agent-chat` (assume the AGENT role and drive route/triage via the LLM), `/agent-run` (deterministic route + triage + close through `process_incident`, no LLM/key), and `/policies [name]` to view/select mock `DynamicPolicies` (`default`/`no-assign`/`generous`) that stand in for Managed Agents memory. The existing `/agent` still runs only the close-stale pass. The selected mock policy is injected into the AGENT system prompt (so the LLM passes it to `process_incident`) and used by the offline `run_agent_route_and_triage`. Set sample authorization levels in `adjusters.json` (`sam`→MEDIUM, `jane`→HIGH; `jaime` stays LOW) so routing has targets.

### Changed
- 2026-07-24: `process_incident` no longer escalates unassigned incidents to the `"unassigned"` placeholder. When a claim isn't auto-approved it now branches on `DynamicPolicies.can_assign`: an unassigned incident is routed to a suitable adjuster when `can_assign` is True (and left untouched when False or when no adjuster has sufficient authorization); an incident that already has an adjuster still escalates. Added `can_assign` (default `False`) and cost limits `authorization_low`/`authorization_medium`/`authorization_high` (500/1500/5000) to `DynamicPolicies`, plus `DynamicPolicies.required_authorization(cost)` mapping a cost to the minimum `AuthorizationLevel`. Routing picks the lowest-authorization adjuster that qualifies via a new internal `tools._select_adjuster` helper.

### Added
- 2026-07-24: Expanded the `AGENT` maintenance persona to route and triage claims. Rewrote `_AGENT_PROMPT` (`roles.py`) to delegate routing to `process_incident`: the agent lists `"unassigned"` incidents and calls `process_incident` with `{"can_assign": true}`, which auto-approves, routes to an authorization-appropriate adjuster, or escalates — calling it again on a just-assigned incident to reach a decision, and reporting any claim whose cost exceeds every authorization limit as needing manual routing. (Supersedes the earlier draft that had the agent route manually with hardcoded cost bands; the authoritative limits now live in `DynamicPolicies`.) Granted the `AGENT` role `process_incident` and a new `list_adjusters` tool.
- 2026-07-24: `list_adjusters()` in `tools.py` — returns the full adjuster roster so the agent can route by `authorization_level`. Registered in `AGENT_TOOLS`.
- 2026-07-23: `assign_incident(incident_id, adjuster_id)` in `tools.py` — reassigns an incident to an adjuster and appends a date-stamped history note. Validates the adjuster exists (returns `None` if the adjuster or incident is unknown). Registered in `AGENT_TOOLS` and granted to the `AGENT` role (`roles.py`), along with `find_adjuster`/`get_adjuster_details` so the agent can pick an assignee. Regenerated `agent_tools_schema.json`.
- 2026-07-23: `AuthorizationLevel` (`StrEnum`: `LOW`/`MEDIUM`/`HIGH`) in `data_entities.py`, and an `authorization_level` field on `Adjuster` (defaults to `LOW`) for the agent to use when assigning incidents to adjusters. `Adjuster.__post_init__` coerces a plain string (as loaded from JSON) into the enum; existing `adjusters.json` entries default to `LOW`.
- `.env` file with an `ANTHROPIC_API_KEY` placeholder and a matching `.env.example`.
- `.env` loading in `manaul_setup.py` via `python-dotenv` (`load_dotenv()`), so the Anthropic SDK picks up the key automatically.
- Guard in `main()` that exits with a clear message when `ANTHROPIC_API_KEY` is missing or still the placeholder.
- `.gitignore` excluding `.env`, `.key`, and Python artifacts.

### Fixed
- Corrected the import of `BetaEnvironment` and `BetaManagedAgentsAgent` in `manaul_setup.py`; they live under `anthropic.types.beta`, not `anthropic.types`.

### Changed
- 2026-07-23: `cma.py` now assigns a different model per role: replaced the single `MODEL = "claude-opus-4-8"` constant with a `MODEL_BY_ROLE` mapping (`INSURER` → `claude-haiku-4-5`, `ADJUSTER` → `claude-sonnet-5`, `AGENT` → `claude-opus-4-8`), used when creating each role's agent in `setup()`.
- Moved the Anthropic API key from the plaintext `.key` file into `.env`.
- 2026-07-22: Converted `ReviewerDetails` in `tools.py` to a `@dataclass` (replacing a broken hand-written `__init__`) and removed the unused `sys`/`os` imports.
- 2026-07-23: Completed the insurance incident mock in `tools.py` — `Adjuster`/`Incident` dataclasses, `ReportType` (`IntFlag`) and `ReportStatus` (`StrEnum`) enums, and JSON persistence (`save_incident`, `get_incident_details`, `load_incidents`, `create_incident`) storing incidents keyed by `case_id`.
- 2026-07-23: Added `adjusters.json` with three adjusters (`jaime`, `sam`, `jane`) keyed by first name, plus `load_adjusters()`/`find_adjuster()` in `tools.py`. Rewired `get_adjuster_details(case_id)` to resolve a case's adjuster id against the adjusters file (returns `None` when the case or adjuster is missing).
- 2026-07-23: Reworked the data model in `tools.py`. Added `Insurer` (policyholder: id/full_name/address/phone_number) and `Policy` (policy_type/insurer_id/effective_date/expiration_date/premium/id) dataclasses. Changed `PolicyType` from `IntFlag` to a single-value `StrEnum`. Renamed `Incident.case_id` → `id` and added a `policy_id` link (keeping `insurer_id` as a secondary link). Added `load_insurers()`/`find_insurer()`, `load_policies()`/`find_policy()`, and a `_policy_from_dict` parser. Fixed the incident (de)serialization to match the normalized id-based shape and corrected `get_adjuster_details`.
- 2026-07-23: Added sample data `insurers.json` (5 policyholders) and `policies.json` (5 policies) with policies cross-referencing insurers by id.

- 2026-07-23: Renamed `Adjuster.local_time` → `timezone` (IANA name, e.g. `America/Los_Angeles`) and populated it in `adjusters.json`.
- 2026-07-23: Added sample data `incidents.json` with four incidents cross-referencing existing policies, insurers, and adjusters by id.
- 2026-07-23: Documented `tools.py` for agent tool use — Google-style docstrings (with `Args`/`Returns`) on every public function, `Attributes` docs on all dataclasses, member descriptions on all enums, and an agent quick-start in the module docstring.
- 2026-07-23: Added optional free-text `preferences` and `history` fields to `Adjuster` and `Insurer`, and an optional `history` field to `Incident`. Updated incident (de)serialization to persist `history` (backward compatible via `.get`) and populated sample values in `adjusters.json`, `insurers.json`, and `incidents.json` (incl. renaming insurer `ins-1002` to "Joseph Okafor" for the goes-by-Joe preferences example).
- 2026-07-23: Added append helpers in `tools.py`: `append_incident_history`, `append_adjuster_history`, `append_insurer_history` (date-stamped, newline-appended log entries) and `append_adjuster_preferences`, `append_insurer_preferences` (un-stamped preference lines). Each loads, mutates, persists, and returns the updated entity (or `None` for an unknown id). Added `_write_adjusters`/`_write_insurers` write-back helpers and `_stamp`/`_append_line` utilities.
- 2026-07-23: Added `set_*` overwrite counterparts for every history/preferences field: `set_incident_history`, `set_adjuster_history`, `set_insurer_history`, `set_adjuster_preferences`, `set_insurer_preferences`. Passing `None` clears the field. Refactored the shared load→mutate→persist plumbing into `_update_incident`/`_update_adjuster`/`_update_insurer` helpers used by both the `append_*` and `set_*` tools.
- 2026-07-23: Added an incident resolution/approval workflow to `tools.py`:
  - New `Resolution` StrEnum (APPROVED/DENIED/INPROGRESS/DISPUTED) and new `Incident` fields `resolution` (default INPROGRESS) and `estimate_cost` (int dollars, default 0), with backward-compatible (de)serialization. `create_incident` gained an `estimate_cost` parameter.
  - New `Escalation` dataclass and `escalations.json` queue (keyed by incident id), with `load_escalations`/`_write_escalations`.
  - New tools: `update_resolution` (records deciding adjuster; clears the escalation on APPROVED/DENIED), `update_status`, `notify_adjuster`/`notify_insurer`/`notify_update` (log to history, set status NOTIFIED, return a message), `escalate_incident`, `list_escalations`, `is_insurer_upset` (keyword scan of insurer text), and `process_incident` (auto-approve < $300, or < $1000 when the insurer is upset; otherwise escalate).
  - Added `estimate_cost`/`resolution` to the four sample incidents in `incidents.json`.

- 2026-07-23: Defined the agent tool surface in `tools.py`. Added an `AGENT_TOOLS` registry (25 functions) as the single source of truth for what should be exposed to an AI agent. Marked the non-agent functions private by renaming `load_incidents`/`load_adjusters`/`load_insurers`/`load_policies`/`load_escalations`/`save_incident` to `_`-prefixed helpers (agents use the `find_*`/`get_*`/`list_*` tools instead). Exposed `escalate_incident`, `notify_adjuster`, and `notify_insurer`. Added `list_incidents(adjuster_id)` for incident discovery. Updated the module docstring to describe the tool surface.
- 2026-07-23: Changed `approve_incident` to take an `insurer_upset: bool = False` parameter instead of computing sentiment internally (the agent judges from history); removed the `is_insurer_upset` function and `_UPSET_KEYWORDS`.
- 2026-07-23: Split the monolithic `tools.py` into three modules: `data_entities.py` (enums, dataclasses, and public `*_to_dict`/`*_from_dict` serialization), `storage.py` (the `data/` file paths and the load/save/update persistence layer), and `tools.py` (only the agent-callable functions + `AGENT_TOOLS`). Moved the JSON stores into a new `data/` subfolder and pointed all paths there; `escalations.json` is now created there on demand. No change to the agent tool surface (still 25 tools).
- 2026-07-23: Added `Insurer.is_VIP` (bool, default False) and populated the five sample insurers with a mix of True/False. Added `Incident.resolution_reason` (str, optional) with matching (de)serialization.
- 2026-07-24: Added the `ant` CLI / version-controlled control-plane path for the Managed Agents setup. `gen_cma_yaml.py` generates, under `cma/`, the environment YAML, one agent YAML per role (tools + system prompt embedded, generated from the same `cma`/`roles`/`agent_schemas` code so they can't drift), adjuster memory seed files, and `setup.sh`. `setup.sh` applies them via `ant beta:environments/agents create`, provisions and seeds the memory stores via `ant beta:memory-stores`/`:memories` (beta `agent-memory-2026-07-22`, set automatically by the CLI), and writes `data/cma_config.json` — the same config `cma.py` consumes at runtime. Generation is round-trip-validated (YAML reloads to the exact bodies) and the emitted config JSON was verified valid; `cma.setup()` (SDK) and `cma/setup.sh` (CLI) are interchangeable ways to provision the same resources. (PyYAML is a dev-only dependency of the generator, not of the runtime.)
- 2026-07-24: Added a Managed Agents (CMA) front-end, `cma.py`, that uses Managed Agents memory stores for adjuster memory. `setup()` idempotently provisions a cloud environment, one agent per role, and one `memory_store` per adjuster (seeding jaime's policy overrides), persisting IDs in `data/cma_config.json`. `CmaSession` creates a session per role/identity (adjuster sessions attach the adjuster's memory store, mounted at `/mnt/memory/<store>/`, and use an identity-specific `agent_with_overrides` system prompt), streams events, and dispatches `agent.custom_tool_use` events to the role-restricted `tools.py` functions (declared as CMA custom tools). Adjuster agents also get the prebuilt `agent_toolset_20260401` so they can read/write their memory store; the maintenance agent closes stale resolved claims. Reuses `roles`, `agent_schemas`, and the `repl` dispatcher so the hosted and local paths stay in lockstep. Verified by offline inspection (tool defs, prompts, config); run in an MA-enabled workspace via `python3 cma.py` then `/setup`.
- 2026-07-24: Added a role-based CLI REPL (agent chat) front-end and the shared role logic behind it.
  - `data_entities.py`: added `Incident.resolved_date` (UTC datetime, set when a claim is approved/denied) with matching (de)serialization.
  - `tools.py`: `update_resolution` now stamps `resolved_date` on APPROVED/DENIED; added `list_incidents_for_insurer(insurer_id)` and `close_stale_resolved(minutes=30)` (auto-closes claims resolved longer than the threshold), both registered in `AGENT_TOOLS` (now 27 tools).
  - `roles.py`: RBAC shared by the REPL and the agent — per-role tool allow-lists (`ADJUSTER`/`INSURER`/`AGENT`), `tools_for_role`/`schemas_for_role`/`dispatch_table`, and `build_system_prompt` that seeds each persona (adjuster prompt embeds the memory-overridden `DynamicPolicies`; insurer prompt is personalized and can only file/query its own claims).
  - `agent_memory.py`: `data/agent_memory.json` store of per-adjuster `DynamicPolicies` overrides + notes; `effective_policies()` merges them over the defaults. Seeded a sample for `jaime`.
  - `repl.py`: the REPL loop (`/adjuster <name>`, `/insurer <id>`, `/agent`, `/whoami`, `/reset`, `/help`, `/quit`). Uses the Anthropic SDK (`claude-opus-4-8`, adaptive thinking, manual tool loop) with role-filtered schemas and a role-restricted dispatcher; `SYSTEM_PROMPT_EXTRA` is the hook for adding tone. `/agent` runs `close_stale_resolved` deterministically (no API/key needed).
- 2026-07-23: Made `process_incident`/`escalate_incident` accept `policies` as a `DynamicPolicies`, a plain dict, or None. Added `DynamicPolicies.coerce()` to normalize the three (unknown dict keys ignored), so an agent tool-call dispatcher can pass the JSON object straight through without constructing the dataclass. Schema output is unchanged (the object schema still wins over `dict`).
- 2026-07-23: Added `agent_schemas.py`, which introspects `tools.AGENT_TOOLS` (signatures + type hints + Google-style docstrings) to generate Anthropic Messages API tool schemas (`{name, description, input_schema}`). Handles nested dataclass params as object schemas (`DynamicPolicies`), `StrEnum` params as string enums, and `IntFlag` (`ReportType`) as an integer bitmask with member values. Running it writes `agent_tools_schema.json` (25 tools).
- 2026-07-23: Added a `DynamicPolicies` dataclass (`data_entities.py`) holding tunable auto-approve ceilings (`auto_approve`, `vip_auto_approve`, `upset_auto_approve_home`, `vip_auto_approve_home`) plus `agent_override_autoapprove`/`agent_override_autoapprove_reason`. Its `should_auto_approve()` method returns `(approve, reason)` using the highest applicable ceiling (VIP status from the insurer record, HOME policy type, upset flag), with the agent override short-circuiting to approve. Reworked `approve_incident` to take a `DynamicPolicies` (defaulting to `DynamicPolicies()`) and delegate the decision to it, recording the reason on `resolution_reason`; `escalate_incident` also now accepts the policies object. Added an optional `reason` parameter to `update_resolution`. Removed the obsolete `AUTO_APPROVE_UNDER`/`UPSET_APPROVE_UNDER` constants.

### Fixed
- 2026-07-23: Corrected the user-added `Resolution` StrEnum (was using `:` instead of `=` and `auto ()`, so no members were assigned).

### Fixed
- 2026-07-23: Repointed sample `case-3004` from a home policy (`pol-2005`) to an auto policy (`pol-2001`/`ins-1001`) so its auto theft/accident claim sits on matching coverage.
- 2026-07-23: `Policy.id` was declared without a type annotation, so it wasn't a real dataclass field; `_incident_to_dict` referenced the removed `incident.adjuster`; and `get_adjuster_details` used the stale `incident.adjuster.user_id`. All corrected.

### Removed
- Deleted the plaintext `.key` file.

### Dependencies
- Added `python-dotenv`.
